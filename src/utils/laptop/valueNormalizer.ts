
// Re-export all normalizers from the normalizer module
export {
  normalizeBrand,
  normalizeProcessor,
  normalizeGraphics,
  normalizeScreenSize,
  normalizeRam,
  normalizeStorage,
  normalizeModel
} from './normalizer';
