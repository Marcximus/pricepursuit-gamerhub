
/**
 * Re-export from the new parsers directory for backward compatibility
 */
import { parseGeneratedContent } from "./parsers/index.ts";

// Export the main function for backward compatibility
export { parseGeneratedContent };
